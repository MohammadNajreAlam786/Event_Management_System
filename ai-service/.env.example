# AI service environment configuration.
# Copy this file to ai-service/.env and adjust as needed.
# Used when the service is started via `python main.py`.

AI_SERVICE_HOST=127.0.0.1
AI_SERVICE_PORT=8000
